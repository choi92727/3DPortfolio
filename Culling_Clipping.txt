클리핑 하드웨어적으로 일어난다
컬링   소프트웨어적으로 일어난다

클리핑 평면
	버택스 셰이더로 들어가기전에 일어난다
	방향과 값을 줄수 있다.
	프로젝션 변환과 뷰포트 변환 사이에 들어간다

레스터라이징
	알파 기준으로 잘라낸다.
	알파 테스팅 (값이 통과되느냐 마느냐)
	통과가 안되면 폐기가 된다(discard)
	
픽셀 셰이더
	하드웨어를 프로그래밍으로 제어하는것..
	clip(x) x 가 0이라면 0이하의 값은 다 폐기한다.(오차범위를 감안해서 준다)
	0.1 까지 잘라내고 싶다면 clip(x - 0.1)
	